name: CI Frontend

on:
  push:
    branches:
      - develop
      - master
      - "feature/**"
    paths:
      - "src/frontend/**"
      - ".github/workflows/ci-frontend.yml"
  pull_request:
    branches:
      - develop
      - master
    paths:
      - "src/frontend/**"
      - ".github/workflows/ci-frontend.yml"

jobs:
  build-and-test:
    name: Build e Testes
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Cache dependências npm
        uses: actions/cache@v4
        with:
          path: src/frontend/orizon-frontend/node_modules
          key: ${{ runner.os }}-npm-${{ hashFiles('src/frontend/orizon-frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Instalar dependências
        working-directory: src/frontend/orizon-frontend
        run: npm ci

      - name: Build produção
        working-directory: src/frontend/orizon-frontend
        run: npm run build:prod

      - name: Rodar testes unitários
        working-directory: src/frontend/orizon-frontend
        run: npm test

      # TODO Fase 10 — habilitar quando testes E2E existirem
      # - name: Instalar Playwright
      #   working-directory: src/frontend/orizon-frontend
      #   run: npx playwright install --with-deps chromium
      #
      # - name: Rodar testes E2E
      #   working-directory: src/frontend/orizon-frontend
      #   run: npx playwright test